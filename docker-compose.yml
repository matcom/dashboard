version: '3'

services:
  app:
    image: apiad/matcom-dashboard:latest
    build: .
    user: ${USER}
    ports:
      - "8501:8501"
    volumes:
      - ".:/src"
    working_dir: "/src"
    command: [ "streamlit", "run", "dashboard/⭐_dashboard.py" ]
